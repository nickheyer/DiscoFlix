.chatBarContainer#chatBar(
  hx-select-oob="#chatBar"
  hx-swap-oob="outerHTML"
)
  //- BARE IN TAKEOVER — THE GRID ROW COLLAPSES INSTEAD OF SHOWING A DEAD INPUT
  if !activeApp
    .floatingChatBar
      include plusButton
      include chatInput
