if (servers.activeChannel && servers.activeChannel.channel_name)
  input.chatMessageInput(
    placeholder="Message #" + servers.activeChannel.channel_name
  )
else
  input.chatMessageInput(
    placeholder="No servers currently available..."
    disabled=true
  )
