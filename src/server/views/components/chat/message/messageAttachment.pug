//- RENDERS `attachmentList` ({ url, name, contentType }). IMAGES INLINE,
//- EVERYTHING ELSE AS A DOWNLOAD LINK. ONLY http(s) URLS RENDER.
if attachmentList && attachmentList.length
  each att in attachmentList
    - var attUrl = att.url && /^https?:\/\//i.test(att.url) ? att.url : null
    if attUrl && att.contentType && att.contentType.startsWith('image/')
      .chatMessageAttachment
        img(src=attUrl alt=att.name || 'attachment' referrerpolicy="no-referrer")
    else if attUrl
      a.chatAttachmentLink(href=attUrl target="_blank" rel="noopener noreferrer")
        svg.chatAttachmentIcon(viewBox="0 0 24 24" fill="none" stroke="currentColor" aria-hidden="true")
          path(d="M21.44 11.05l-9.19 9.19a6 6 0 0 1-8.49-8.49l9.19-9.19a4 4 0 0 1 5.66 5.66l-9.2 9.19a2 2 0 0 1-2.83-2.83l8.49-8.48" stroke-width="2" stroke-linecap="round" stroke-linejoin="round")
        span #{att.name || 'attachment'}
