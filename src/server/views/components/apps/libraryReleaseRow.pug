//- SINGLE-ROW RESPONSE TO POST /apps/:id/library/item/:itemId/grab - SWAPS
//- THE CLICKED ROW IN PLACE. LOCALS: activeApp, release, itemId
include ../../mixins/releases
+interactiveReleaseRow(release, activeApp.id, itemId)
